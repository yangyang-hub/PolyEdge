# i18n（国际化）

最后更新：2026-07-16

前端继续使用单一中文字典。`src/lib/i18n/dictionaries/console.ts` 覆盖策略、钱包、执行运营、设置、操作反馈和仪表盘；V3 namespace、credential locator 文案和未引用键已删除。`shared.ts` 提供 Cookie session、角色和管理员导航文案。Funding、Markets、Events、Rewards 和旧 generated-copy 字典已删除。新增 admin/following 工作台目前仍有部分硬编码中文，尚未完全纳入字典。

组件从 `@/lib/i18n/dictionaries` 读取 `dictionary`；新增文案应进入对应命名空间，不在组件中重复后端枚举结构。
