# PolyEdge 前端本地开发环境变量。复制为 packages/front/.env。
# Next.js 开发服务器不提供 /api 反向代理，因此直连本机 polyedge-server。
# 请同时把后端 POLYEDGE_PUBLIC_ORIGIN 和 CORS allowlist 配为 http://localhost:33002。
# 生产镜像使用 deploy/.env.front，并保持该值为空以走 Nginx 同源 /api 代理。
NEXT_PUBLIC_POLYEDGE_API_BASE_URL=http://localhost:38001
