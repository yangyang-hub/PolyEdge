# packages/AGENTS.md

`packages/` 同时包含 `packages/backend` Rust 后端 workspace 和 `packages/front` 前端项目。

- 修改 `packages/backend/api/`、`packages/backend/order/` 或 `packages/backend/` 下的 Rust 代码时，遵守 [backend/AGENTS.md](backend/AGENTS.md)。
- 修改 `packages/front/` 下的前端代码时，遵守 [front/AGENTS.md](front/AGENTS.md)。
