# packages/AGENTS.md

`packages/` 包含 `packages/backend` Rust workspace 和 `packages/front` Next.js 控制台。

- 修改 `packages/backend/server/` 或 `packages/backend/crates/` 下的 Rust 代码时，遵守 [backend/AGENTS.md](backend/AGENTS.md)。
- 修改 `packages/front/` 下的前端代码时，遵守 [front/AGENTS.md](front/AGENTS.md)。
