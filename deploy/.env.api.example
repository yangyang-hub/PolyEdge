# PolyEdge API 服务环境变量。
# 与 .env（公共变量）配合使用。

# POLYEDGE_API_BIND：
# API 暴露到宿主机的监听地址。0.0.0.0 表示所有网卡可访问；只给本机反代可改为 127.0.0.1。
# POLYEDGE_API_BIND=0.0.0.0

# POLYEDGE_API_PORT：
# API 暴露到宿主机的端口，映射到容器内 38001。
# POLYEDGE_API_PORT=38001

# POLYEDGE_ORDERBOOK__SERVICE_URL：
# API 访问 orderbook 服务的 HTTP 地址。同服务器部署使用默认值即可。
# 跨服务器部署时改为 orderbook 服务器的实际地址（如 http://192.168.31.10:38002）。
# POLYEDGE_ORDERBOOK__SERVICE_URL=http://localhost:38002

# ── Rewards live snapshot（可选，高风险）──────────────────────────────────
# 当前 /api/v1/rewards-bot handler 会直接从 Polymarket 覆盖 live 账户视图：
# - 仅配置 POLYEDGE_POLYMARKET__ACCOUNT_ID（以及可选 DATA_API_HOST）可读取 positions。
# - balance/open orders 还需要 PRIVATE_KEY、签名类型、对应 FUNDER，以及可选的预配置 CLOB API 凭证。
# 这会让 API 进程持有交易私钥，并违反目标架构中“API 只读 store/cache”的规则。
# 推荐只把私钥放在 .env.worker，并接受 API balance/open orders 暂时显示为零/空。
# 只有明确接受该风险时，才从 deploy/.env.polymarket.example 复制对应账户配置到本文件。
