# PolyEdge API 服务环境变量。
# 与 .env（公共变量）配合使用。

# 可选：宿主机端口绑定。docker-compose.yml 已内置相同默认值。
# POLYEDGE_API_BIND=0.0.0.0
# POLYEDGE_API_PORT=38001

# 可选：Orderbook 服务地址。Docker Compose 已内置默认值。
# POLYEDGE_ORDERBOOK__SERVICE_URL=http://polyedge-orderbook:38002
