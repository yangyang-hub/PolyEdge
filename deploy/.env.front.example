# PolyEdge 前端控制台环境变量。
# 与 .env（公共变量）配合使用。

# POLYEDGE_FRONT_BIND：
# 前端 Nginx 暴露到宿主机的监听地址。0.0.0.0 表示所有网卡可访问。
# POLYEDGE_FRONT_BIND=0.0.0.0

# POLYEDGE_FRONT_PORT：
# 前端控制台暴露到宿主机的端口，映射到容器内 Nginx 80。
# POLYEDGE_FRONT_PORT=33002

# POLYEDGE_API_UPSTREAM：
# 前端 Nginx 代理 /api/v1 请求时使用的后端 API 地址。Compose 内部默认指向 polyedge-api。
# POLYEDGE_API_UPSTREAM=http://polyedge-api:38001
