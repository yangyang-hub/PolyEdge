services:
  polyedge-server:
    build:
      context: ../bin
      dockerfile: ../deploy/server.Dockerfile
    image: ${POLYEDGE_SERVER_IMAGE:-polyedge-server:local}
    restart: unless-stopped
    logging: &default-logging
      driver: json-file
      options:
        max-size: "50m"
        max-file: "3"
    env_file:
      - ${POLYEDGE_SERVER_ENV_FILE:-.env.server}
    ports:
      - "${POLYEDGE_SERVER_BIND:-0.0.0.0}:${POLYEDGE_SERVER_PORT:-38001}:38001"
    command: ["polyedge-server"]
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:38001/healthz >/dev/null"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 20s

  polyedge-front:
    build:
      context: ../packages/front
      dockerfile: Dockerfile
    image: ${POLYEDGE_FRONT_IMAGE:-polyedge-front:local}
    restart: unless-stopped
    logging: *default-logging
    env_file:
      - ${POLYEDGE_FRONT_ENV_FILE:-.env.front}
    ports:
      - "${POLYEDGE_FRONT_BIND:-0.0.0.0}:${POLYEDGE_FRONT_PORT:-33002}:80"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "wget -qO- http://127.0.0.1/healthz >/dev/null",
        ]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 30s
